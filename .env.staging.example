# Staging environment variables
# The template initializes .env.staging from this file once, then leaves both files project-owned.
# Add project-specific placeholders below; never commit real secrets.
# PROJECT_VARIABLE=
