# Explicit database URL; each store, worktree, test and shadow database must be isolated.
# SQLite: file:./dev.sqlite (relative to this package)
# PostgreSQL: postgresql://user:password@127.0.0.1:5432/app?schema=worktree_unique
{{storeEnv}}=
TEST_{{storeEnv}}=
SHADOW_{{storeEnv}}=
